# Секретный токен: его же вставляешь в Быструю команду и в дашборд
API_TOKEN=change-me-to-a-long-random-string
# Часовой пояс для группировки по дням/месяцам
TIMEZONE=Asia/Almaty
# Валюта по умолчанию, если в сумме её не удалось распознать
DEFAULT_CURRENCY=KZT
# Путь к базе
DB_PATH=data/expenses.db
